{% extends 'base.html.twig' %}

{% block body %}
<h1>Add a New Dish</h1>

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.ingredients) }}

    <button class="btn">Add Dish</button>
{{ form_end(form) }}
{% endblock %}
